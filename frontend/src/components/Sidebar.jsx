import { NavLink } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'
const links = { admin: [['/admin', 'Overview'], ['/admin/departments', 'Departments'], ['/admin/classes', 'Classes'], ['/admin/subjects', 'Subjects'], ['/admin/faculty', 'Faculty'], ['/admin/students', 'Students'], ['/admin/corrections', 'Corrections'], ['/admin/reports', 'Reports']], faculty: [['/faculty', 'Overview'], ['/faculty/sessions', 'My sessions'], ['/faculty/mark', 'Mark attendance'], ['/faculty/correction', 'Request correction'], ['/faculty/reports', 'Reports']], student: [['/student', 'Overview'], ['/student/attendance', 'My attendance'], ['/student/calendar', 'Calendar']] }
export default function Sidebar() { const { role } = useAuth(); return <aside className="sidebar"><div className="sidebar-caption">Workspace</div>{links[role]?.map(([to, label]) => <NavLink key={to} to={to} end={to === `/${role}`} className={({ isActive }) => isActive ? 'nav-link active' : 'nav-link'}><span className="nav-dot" />{label}</NavLink>)}</aside> }
