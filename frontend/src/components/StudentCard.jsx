export default function StudentCard({ subject, percentage, meta }) { return <article className="metric-card"><div className="card-kicker">{subject}</div><div className="metric-value">{percentage}%</div><div className={`progress progress-${percentage < 75 ? 'low' : percentage < 90 ? 'mid' : 'high'}`}><span /></div><div className="card-meta">{meta || 'Attendance rate'} <span className={percentage < 75 ? 'text-danger' : 'text-good'}>{percentage < 75 ? 'Needs attention' : 'On track'}</span></div></article> }
