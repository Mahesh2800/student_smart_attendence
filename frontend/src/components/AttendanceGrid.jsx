import { useEffect, useState } from 'react'
const statuses = ['Present', 'Absent', 'Late', 'Excused']
export default function AttendanceGrid({ students = [], onSave }) { const [values, setValues] = useState({}); useEffect(() => setValues(Object.fromEntries(students.map((student) => [student.id, 'Present']))), [students]); const setAll = (status) => setValues(Object.fromEntries(students.map((student) => [student.id, status]))); return <div><div className="grid-actions"><button className="button secondary" onClick={() => setAll('Present')}>Mark all present</button><span>{students.length} students</span></div><div className="table-wrap"><table><thead><tr><th>Student</th><th>Roll number</th><th>Status</th></tr></thead><tbody>{students.map((student) => <tr key={student.id}><td>{student.name || student.username}</td><td>{student.roll_no}</td><td><select value={values[student.id] || 'Present'} onChange={(event) => setValues({ ...values, [student.id]: event.target.value })}>{statuses.map((status) => <option key={status}>{status}</option>)}</select></td></tr>)}</tbody></table></div><button className="button primary" onClick={() => onSave?.(Object.entries(values).map(([student_id, status]) => ({ student_id: Number(student_id), status })))}>Save attendance</button></div> }
