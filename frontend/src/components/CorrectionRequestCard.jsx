export default function CorrectionRequestCard({ item, onResolve }) { return <article className="list-card"><div><strong>{item.record?.student_name || item.student_name || 'Attendance record'}</strong><p>{item.reason}</p></div><div className="list-card-end"><span className={`status ${item.approval_status?.toLowerCase()}`}>{item.approval_status}</span>{onResolve && item.approval_status === 'Pending' && <div className="button-row"><button className="button primary small" onClick={() => onResolve(item.id, 'approve')}>Approve</button><button className="button danger small" onClick={() => onResolve(item.id, 'reject')}>Reject</button></div>}</div></article> }
