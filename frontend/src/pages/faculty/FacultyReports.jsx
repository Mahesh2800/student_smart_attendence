import { useEffect, useState } from 'react'
import { getLowAttendance } from '../../api/studentService'
import Loader from '../../components/Loader'

export default function FacultyReports() { const [threshold, setThreshold] = useState(75); const [items, setItems] = useState(null); useEffect(() => { getLowAttendance(threshold).then(({ data }) => setItems(data)).catch(() => setItems([])) }, [threshold]); return <><div className="page-heading"><div><span className="eyebrow">FACULTY REPORTS</span><h1>Attendance watchlist</h1><p>Filter students below your chosen attendance threshold.</p></div><label className="threshold-control">Threshold %<input type="number" min="1" max="100" value={threshold} onChange={(event) => setThreshold(event.target.value)} /></label></div><div className="panel table-panel">{items === null ? <Loader /> : <div className="table-wrap"><table><thead><tr><th>Student</th><th>Roll number</th><th>Overall attendance</th><th>Subject breakdown</th></tr></thead><tbody>{items.length ? items.map((item) => <tr key={item.student_id}><td>{item.name || 'Student'}</td><td>{item.roll_no}</td><td><span className="status absent">{item.percentage}%</span></td><td>{Object.entries(item.subjects || {}).map(([subject, percentage]) => `${subject}: ${percentage}%`).join(' · ')}</td></tr>) : <tr><td colSpan="4">No students match this threshold.</td></tr>}</tbody></table></div>}</div></> }
