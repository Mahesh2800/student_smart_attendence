import { useEffect, useState } from 'react'
import { getLowAttendance } from '../../api/studentService'
import Loader from '../../components/Loader'
export default function AdminDashboard() { const [low, setLow] = useState(null); useEffect(() => { getLowAttendance().then(({ data }) => setLow(data)).catch(() => setLow([])) }, []); return <><div className="page-heading"><div><span className="eyebrow">ADMIN CONSOLE</span><h1>Good morning, admin.</h1><p>Keep a pulse on attendance health across the college.</p></div><span className="date-chip">Live overview</span></div>{low === null ? <Loader /> : <div className="dashboard-grid"><div className="panel highlight"><span className="eyebrow">LOW ATTENDANCE</span><strong className="big-number">{low.length}</strong><p>students currently need an intervention</p><a href="/admin/reports" className="text-link">Open report →</a></div><div className="panel"><span className="eyebrow">TODAY'S RHYTHM</span><div className="stat-row"><strong>94.2%</strong><span>average attendance</span></div><div className="sparkline"><i /><i /><i /><i /><i /><i /><i /></div></div><div className="panel wide"><span className="eyebrow">OPERATING PRINCIPLE</span><h2>Make the next conversation easier.</h2><p>Use the attendance signal to find students early, review corrections with context, and keep the classroom record trustworthy.</p></div></div>}</> }
