import { useEffect, useState } from 'react'
import { getRecords } from '../../api/attendanceService'
import Loader from '../../components/Loader'
export default function MyAttendance() { const [items, setItems] = useState(null); useEffect(() => { getRecords().then(({ data }) => setItems(data.results || data)).catch(() => setItems([])) }, []); return <><div className="page-heading"><div><span className="eyebrow">STUDENT HUB</span><h1>Attendance history</h1><p>Every marked session, with its context.</p></div></div><div className="panel table-panel">{items === null ? <Loader /> : <div className="table-wrap"><table><thead><tr><th>Date</th><th>Subject</th><th>Roll number</th><th>Status</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td>{item.session_date || item.session}</td><td>{item.subject_name || 'Subject'}</td><td>{item.roll_no}</td><td><span className={`status ${item.status?.toLowerCase()}`}>{item.status}</span></td></tr>)}</tbody></table></div>}</div></> }
